<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.zaandam">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".DirectionMapActivity" />
        <activity android:name=".DirectionActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".ShowResultsOnMapActivity" />
        <activity android:name=".ShowResultsActivity" />
        <activity android:name=".ChoosePOICategory" />
        <activity android:name=".GeocodingActivity" />
        <activity
            android:name=".DisplayMapActivity"
            android:label="@string/title_activity_display_map"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectLanguageActivity" />
    </application>

</manifest>